{
  "mcpServers": {
    "librarian": {
      "command": "sh",
      "args": [
        "-c",
        "exec \"$(command -v python3 || command -v python)\" \"$(git rev-parse --show-toplevel)/librarian_agent/src/mcp_server.py\" --serve"
      ],
      "env": {},
      "$comment": "sh and git are both present wherever this repository was cloned, including Windows with Git for Windows. python3 is NOT: a stock Windows install provides `python` and no `python3`, so the interpreter is resolved rather than named (2026-09-20)."
    }
  }
}
