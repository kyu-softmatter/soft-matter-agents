{"at":"2026-09-18T11:10:00Z","kind":"abandoned_attempt","qid":"lib-task-001","detail":{"qid_note":"the librarian seat has no questions, so there is no qid to write. This is the task id standing in, said out loud rather than left to look like a question. The record shape assumes a question-scoped seat; raised with manager-librarian.","what":"tried to land the DMD control-path and light-engine facts as kb entries, then abandoned it and left them in kb/staging/","why_wrong":"there was no source kind for a measurement made in another project. `measured:` derives E1, which 10.3 rule 1 forbids for a run taken elsewhere; `spec:` and `operator_read:` were the only other E3 prefixes and neither describes a scripted run. An entry with no `source` is counted by check 43 as still self-reported, so writing one anyway would have traded a false prefix for a false pass.","caught_by":"reading SOURCE_GRADE before writing the entry, not by a check -- nothing would have failed if I had used spec:","outcome":"the facts went into the staging table, which takes free-form source strings, and the missing prefix went up to manager-librarian. `prior_run:` and `literature:` now exist (877d652), so the entries can be written.","shape_to_remember":"when the only way to record something is to mislabel it, the label is the bug. Reaching for the nearest prefix would have put a vendor-specification grade on a scripted run and nothing downstream would ever have asked."}}
{"at":"2026-09-18T11:45:00Z","kind":"deviation","qid":"mic-20260918-001","detail":{"what":"edited kb/entries/tau_d.json without first checking who was pinned to the store, moving kb_version from kbv-9bc3910f1886 to kbv-ec984bc852e7","why_wrong":"librarian_agent/CLAUDE.md says to pin kb_version for a whole fan-out and not to add entries mid-flight, because siblings reading different KBs breaks determinism and the difference is itself a channel between them. The rule says `add`; the hash is over every entry, so an EDIT moves it exactly the same way. mic-20260918-001 was mid-fan-out at the time -- two of seven axis cards, no plan, no synthesis -- and the server refuses a stale pin by design, so the remaining axes are blocked until that seat re-pins.","blast_radius":"19 kb_refs across two agents went PENDING under check 25. The actual content difference between the two versions is one entry, and no card in mic-20260918-001 cites it, so re-pinning that fan-out needs no re-derivation.","caught_by":"running the validator after committing, not before editing","outcome":"both affected seats told directly; the simulation question was already complete and correctly keeps its old pin. The practice adopted: check pins first, batch entry edits, announce before moving.","shape_to_remember":"the rule named the common case (adding) and the hash does not care which case it is. A rule written about one verb is worth re-reading as a rule about the effect."}}
