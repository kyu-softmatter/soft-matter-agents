{
  "source_id": "src_prior_mm_device_reported_20260903",
  "kind": "prior_run",
  "identifier": "agentic-microscope at 34a432c: data/light_sources.yaml (the `device_reported:` and `driving_it:` blocks, both dated 2026-09-03 and marked read live over MMCore), hardware/microscope.py, hardware/focus.py, config/micromanager/make_single_cam_cfg.py and the config/session and config/lunf call sites.",
  "accessed": "2026-09-20",
  "grade_rule": "E3, grade_tag absent -- `prior_run` is a source prefix with no place in the E3 grade_tag enum, which is raised (same as src_prior_filter_records). 10.3 rule 1 caps a configuration read elsewhere at E3: there is no run_id from this system and that machine's wiring at that moment is not this machine's now.",
  "transfer_ruling": "10.2.1 TRANSFER. Rows: 10.2's `hardware control paths -- driver call style, channels, read-back`, and `the initial contents of the device registry ... per-device control channels`. Both are marked set. NOT the device-specification row, which waits on envelope/safety.json because a spec figure not placed beside a limit gets used as one. A property's DOMAIN is not a limit -- 0-1000 is what the adapter accepts, and what may be commanded is envelope/safety.json's and a person's. Slot: KB entry plus the device registry in kb/staging/, both named in the 2026-09-18 amendment. Rule: 10.3 rule 1.",
  "what_crossed_and_from_where": "Only values in a machine-readable position, per 10.3 rule 3. The MM labels come from call sites and dict constants -- `CAMERAS = {\"Kinetix_blue\": \"Camera-1\", \"Kinetix_red\": \"Camera-2\"}`, `core.getStateLabel(\"Nosepiece\")`, `sp(\"LightEngine\", \"State\", \"0\")` -- and the scales and property shapes from the `device_reported:` and `driving_it:` YAML blocks. NOT from the module docstring, which names the engines `SpectraIII/AuraIII` and is prose; those are registry names and are not the MM labels.",
  "what_did_not_cross": "The .cfg file itself: DISCARD, and manager-microscope asked for labels rather than the file for the right reason -- a cfg is one machine's wiring at one moment. The line centre wavelengths (365/440/488/514/561/594/640 and 405/440/488/561/640): DISCARD, the file says twice that they were read from NIS-Elements diagram icons and that the device contradicts them. The 2026-09-06 and 2026-09-03 bead readings, ADU counts and bleaching percentages: DISCARD, instrument-specific with no slot here. Their failure narratives are consulted as a failure list only; entering one in kb/lessons/ needs 8.2's evidence id, n and falsifier, which this pass did not do.",
  "note": "The entries built from this each say what would raise them: every one becomes `operator_read:` at E3 with an honest source the moment somebody runs getLoadedDevices() and getDevicePropertyNames() on THIS stand and writes down what came back. The grade does not rise past E3 by that -- both are E3 -- but the source stops being another machine's."
}
