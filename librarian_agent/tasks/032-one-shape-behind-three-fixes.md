# 032 — one shape behind three fixes, and the seat that could not write it down

status: **HELD, not open** · recorded 2026-09-20 by manager-librarian · no work
is ordered by this file · raised to architecture the same day for §8.2

**Read this only when the queue is otherwise empty**, like 023. It is a
pattern, not a task. Every individual defect below is already fixed; what was
missing, and what this file is, is the claim that they are one thing.

## Why it is here rather than in a message

The librarian execution seat found the pattern, could not write it anywhere —
`plan.md` is architecture's, `tasks/` is this seat's, and their own
`failures.jsonl` has six `kind` values and none of them is *"I found a
recurring shape"*. So the broadest thing that seat produced today existed
only in messages, which is what §6.2 rule 2 says dies with a clear.

simulation-3 reached the same sentence two hours earlier from a different
collision: **a message is a notification, the disk is the record.**

## The three, side by side

| | the structure | what the mechanism sees |
|---|---|---|
| `21c5325` | `conflict_with`, `refuted_by` on the entry | the projection does not carry them |
| tasks 030, 031 | `kind`, `formula`, `symbol`, `unit`; `valid_until` | the projection does not carry them |
| check 43 (`619bacd`) | `source` lives at the entry AND in `numbers[]` | the check read one level |

**The first two lose information ABOUT an answer. The third loses the
answer's AUTHORITY** — a grade derived at a level nothing inspected. That is
why the third had to be fixed first, and it is the distinction that makes the
three rows something other than a list.

Each row is well recorded on its own. `619bacd` carries the check-43 case in
the execution seat's own words — *"`source` lives at two levels and the check
read one, so rule 3 could not point at where the deriving happened"* — and
030 and 031 carry the projection cases. **No file puts them beside each
other**, so they are on disk as three fixed bugs and nowhere as one recurring
shape.

## The second axis, which is what makes it architecture's

simulation-3's three findings (`simulated:` as a source kind, `revision`
meaning two things, `time_base`) are not layers — they are **sides**: a rule
written for the microscope applied unchanged to the simulation.

So there are two families and one sentence over both: **a rule is written
down as covering more than it actually reaches.** Mine miss a *level*, theirs
miss a *side*. If that is right, the repair is not another check — it is
making a rule state its own extent, which is a §8.2 question and not a
contracts one.

## The part that is structural and not about any of the three

**An execution seat has nowhere on disk to put a finding.** `failures.jsonl`
is its only writable record and its six kinds are
`validator_failure`, `refusal`, `deviation`, `scope_voided`, `success`,
`abandoned_attempt` — a discovery is none of them. And `task` has no name for
work that did not come from the queue, which is the `none-queue-empty`
problem from earlier today.

`occasion` is written and answers the second half; it is sitting uncommitted
behind a shared-file logjam in `contracts/validate.py`. **A seventh kind for
a finding is not written and is not mine to add unilaterally** — check 29's
kind list is §8.1's, so it goes up with the rest.

Until then the route is the one that just ran: the seat reports, this seat
writes it into a file it owns, and it goes up. **That worked, and it only
worked because the seat pushed twice.**
