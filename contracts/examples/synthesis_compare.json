{
  "card": "synthesis",
  "schema_version": "0.1",
  "id": "synthesis-mic-20260917-002-r1",
  "qid": "mic-20260917-002",
  "thread": "solo-mic-20260917-002",
  "round": 0,
  "revision": 1,
  "author": "microscope_agent",
  "created_at": "2026-09-17T10:10:00Z",
  "status": "VALIDATED",
  "configs_screened": ["brightfield"],
  "per_config": [
    {
      "config": "brightfield",
      "empty": false,
      "intersection": [
        { "parameter": "exposure_time", "unit": "s", "min": 0.001, "max": 0.05, "basis": ["exposure_time_min", "exposure_time_max"], "precision": "order_of_magnitude" },
        { "parameter": "temperature_tolerance", "unit": "K", "max": 0.5, "basis": ["temperature_tolerance_max"], "precision": "order_of_magnitude" }
      ],
      "axis_files": ["axis_compare_a1.json", "axis_compare_a5.json"]
    }
  ],
  "chosen_config": "brightfield",
  "operating_point": [
    { "parameter": "exposure_time", "number": "exposure_time_chosen" }
  ],
  "priority_used": ["physical_feasibility", "target_accuracy", "evidence_grade", "cost"],
  "priority_source": "goal_card",
  "rejected": [],
  "tie_break": "no tie: one configuration, and the exposure is fixed across both arms because the purpose is comparison",
  "numbers": [
    { "name": "exposure_time_min", "value": 0.001, "unit": "s", "source": "assumed:a_exp_min", "grade": "E5", "precision": "order_of_magnitude", "origin": "axis_compare_a1.json#exposure_time_min" },
    { "name": "exposure_time_max", "value": 0.05, "unit": "s", "source": "assumed:a_exp_max", "grade": "E5", "precision": "order_of_magnitude", "origin": "axis_compare_a1.json#exposure_time_max" },
    { "name": "temperature_tolerance_max", "value": 0.5, "unit": "K", "source": "assumed:a_temp_tol", "grade": "E5", "precision": "order_of_magnitude", "origin": "axis_compare_a5.json#temperature_tolerance_max" },
    { "name": "exposure_time_chosen", "value": 0.007, "unit": "s", "source": "computed:geometric_middle", "grade": "E5", "precision": "order_of_magnitude", "formula": "(exposure_time_min*exposure_time_max)**0.5", "inputs": ["exposure_time_min", "exposure_time_max"] }
  ],
  "assumptions": [
    { "rationale_id": "a_exp_min", "statement": "Carried from the brightfield signal axis: read noise floors the exposure at about a millisecond.", "numbers": ["exposure_time_min", "exposure_time_chosen"], "falsifier": "a usable centroid at a shorter exposure" },
    { "rationale_id": "a_exp_max", "statement": "Carried from the brightfield signal axis: blur crosses a pixel in tens of milliseconds.", "numbers": ["exposure_time_max", "exposure_time_chosen"], "falsifier": "a measured blur below a pixel at a longer exposure" },
    { "rationale_id": "a_temp_tol", "statement": "Carried from the time-stability axis: half a kelvin of drift during a record keeps the two arms separable.", "numbers": ["temperature_tolerance_max"], "falsifier": "arm-to-arm scatter larger than the drift effect" }
  ],
  "kb_refs": [],
  "degraded": ["librarian_agent"]
}
