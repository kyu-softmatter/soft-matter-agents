# check 74 — a round reference that is not one

`from_round` is prose here. The form is `thr-<thread>:r<N>`, and a field that
cannot be parsed cannot be resolved -- so the card claims to stand on a round
and nothing can say which.
