{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "goal.schema.json",
  "title": "goal \u2014 why we are asking, and what we want to know (4.5.1, 5.1)",
  "allOf": [
    {
      "$ref": "common.schema.json#/$defs/common_head"
    }
  ],
  "properties": {
    "card": {
      "const": "goal"
    },
    "purpose": {
      "enum": [
        "screen",
        "characterize",
        "compare",
        "verify",
        "troubleshoot",
        "feed"
      ],
      "description": "Cannot be inferred; a missing purpose is a question for the human (4.5.1)"
    },
    "intent": {
      "enum": [
        "explore",
        "confirm"
      ],
      "description": "Defaults from purpose. Differing from the default requires intent_rationale (check 32)."
    },
    "intent_rationale": {
      "type": "string",
      "minLength": 10
    },
    "observable": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "definition": {
          "type": "string",
          "minLength": 20,
          "description": "Kept for now and no longer required: the definition lives in contracts/observables.json, and a card that restates it holds a second copy that drifts -- eight cards had already drifted from the vocabulary by 2026-09-18. Name the observable and the definition, the estimator and the window come with it by reference. This property is due to be removed once every card has dropped it; 5.4 still asks for the definition and that sentence is the architecture seat's to settle before then."
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "targets": {
      "type": "array",
      "description": "What accuracy is wanted. In explore mode, stated in decades. A target is a DECISION and not a claim about the world, so it carries no source and no grade -- the same reason a ceiling in envelope/safety.json carries none. A grade measures how far a claim can be trusted; a decision is correct by being made, and grading a person's own goal E5 would say they might be misremembering what they want. Inline rather than by reference so that a grade is not merely absent but inexpressible: an opt-in guard is not a chokepoint (2.1 rule 9).",
      "items": {
        "oneOf": [
          {
            "$ref": "common.schema.json#/$defs/target"
          },
          {
            "$comment": "SUPERSEDED, and deleted when no card still using it is pinned. This form names an entry in numbers[], which P2 makes four-part, so it forces a source and a grade onto a decision -- what the inline form exists to stop. MIGRATION IS FORWARD ONLY (5.3.1, 3646fb7): moving a field changes the card's hash, and a hash someone recorded is a hash someone recorded. PINNED MEANS TWO THINGS, and the second was missed on the first survey: an approval pins a plan (check 7), and a DELIVERED ROUND pins its source card in bridge/threads/<thread>/r<N>_hashes.json (check 8). The second needs no approval to exist and its failure lands on the bridge's ledger rather than on the card that moved -- manager-simulation found it by checking its own tree instead of trusting this comment. Both have the same exit: the next revision. Check 8 skips a source whose revision has moved on, and an approval covers the revision it names. REMOVAL CONDITION: no unmigrated card is pinned by either -- not 'every card has moved', which cannot become true without asking a person to re-approve work they already approved. STATE ON 2026-09-19: seven cards in four groups, and a group moves together, because check 12 makes a plan's carried number cite the goal's and check 52 compares a carried target against the goal's. Migrated: mic-20260917-002. Frozen by an approval: mic-20260917-001 (contracts/examples/goal.json, plan_microscope_mic-20260917-001.json, result.json), since plan_approval.json pins plan-mic-20260917-001-r1. Frozen by a round ledger at revision 1: sim-20260917-001 (goal and plan_simulation_sim-20260917-001.json), which moves at revision 2, already scheduled as the rerun with the librarian on. Free: mic-20260918-001 and mic-20260919-001, goal only. manager-bridge.",
            "type": "object",
            "properties": {
              "metric": {
                "type": "string"
              },
              "number": {
                "type": "string"
              },
              "kind": {
                "enum": [
                  "uncertainty",
                  "decade_resolution",
                  "detection"
                ]
              }
            },
            "required": [
              "metric",
              "number",
              "kind"
            ],
            "additionalProperties": false
          }
        ]
      }
    },
    "priority": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Ordered. Safety is deliberately absent: it is a constraint, not a tradeable item (2.1)."
    },
    "compare_variable": {
      "type": "string",
      "description": "required when purpose is compare: the one parameter allowed to differ between arms (check 34)"
    },
    "requested_by": {
      "$ref": "common.schema.json#/$defs/agent"
    },
    "constraint_notes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Free-text limits from the human. Named apart from the interval lists elsewhere: same word, different kind of thing."
    },
    "configuration_preference": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_]*$"
      },
      "description": "An ordered tie-break from the person, read by S3.0 ONLY when the cap is unresolved (4.5.1 branch c). It settles which of several capable configurations to spend the fan-out on. It never overrides a capability refusal: a preference naming a configuration the screen rejected, or one absent from the table, is recorded and refused rather than honoured, and screening stops at that entry instead of sliding to the next -- a card asking for a configuration the instrument cannot use is a card to fix, and quietly granting its second choice hides that. It is not evidence: it raises no grade, supports no number, is not an assumption with a falsifier, and S4 may not cite it."
    },
    "sample_contrast_provenance": {
      "type": "object",
      "description": "Where sample_contrast came from. It is a claim about the world -- these beads carry a fluorophore -- so it is graded like any other claim (P2). An operator reading an instrument and an operator recalling a preparation are not the same source and do not get the same grade: a statement from memory is E5. Required in practice whenever sample_contrast is present; left optional for one step only, because making it required today would fail a goal card already committed in another seat's path. Tighten once that card carries it.",
      "properties": {
        "source": {
          "$ref": "common.schema.json#/$defs/source"
        },
        "grade": {
          "$ref": "common.schema.json#/$defs/grade"
        }
      },
      "required": [
        "source",
        "grade"
      ],
      "additionalProperties": false
    },
    "configuration_preference_by": {
      "type": "object",
      "description": "Who chose the tie-break, and when. Deliberately NOT a source and a grade: a preference is not a claim about the world, so there is nothing to grade and nothing to falsify (4.5.1 c). What it needs is attribution, so that a later reader can ask the person rather than mistake the choice for a finding. `reason` is optional and stays empty when none was given -- an invented reason would be worse than the silence.",
      "properties": {
        "who": {
          "type": "string",
          "minLength": 1
        },
        "at": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "who",
        "at"
      ],
      "additionalProperties": false
    },
    "sample_contrast": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "common.schema.json#/$defs/contrast"
      },
      "description": "What this sample offers, as a set. A configuration survives the contrast screen when its requires_contrast is one of these -- membership, not equality. A set because a fluorescent tracer still has refractive-index contrast and so provides both, which is why equality would drop brightfield from a fluorescent question. S3.0 can evaluate this before S3 runs, which none of the priority terms can (4.5.3)."
    },
    "from_round": {
      "type": "string",
      "pattern": "^thr-[a-z0-9-]+:r[1-9][0-9]*$",
      "description": "Present when this goal was written from a delivered round (5.1): the thread and round it came from. It is how the bridge learns the round was taken. The bridge already reads the executing agents' questions/ (6.2-3), so this closes the loop with no write across a boundary and no second copy of whose turn it is -- the thread ledger stays the only place that says that."
    },
    "selectors": {
      "type": "array",
      "items": {
        "$ref": "common.schema.json#/$defs/selector"
      },
      "description": "Discrete settings the PERSON chose, arriving here because this is where a person's decision enters the pipeline. Same $defs as the plan's, deliberately: a selector the person fixes and a selector the plan commands are the same kind of thing, and S5 carries one into the other rather than re-deriving it.\n\nAdded 2026-09-21, one stage late. `system_configuration.selectors` landed on the plan the same day and the goal did not get one, so the stage that EMITS a selector had a slot and the stage that RECEIVES the person's choice did not. S5 stopped on exactly that: the person had chosen which camera collects, and the card could not hold the answer. Third instance in two days of a rule with no field -- `run_log.verification`, `plan.preconditions`, and this."
    }
  },
  "required": [
    "purpose",
    "intent",
    "observable",
    "requested_by"
  ],
  "unevaluatedProperties": false
}
