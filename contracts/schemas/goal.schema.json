{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "goal.schema.json",
  "title": "goal — why we are asking, and what we want to know (4.5.1, 5.1)",
  "allOf": [
    {
      "$ref": "common.schema.json#/$defs/common_head"
    }
  ],
  "properties": {
    "card": {
      "const": "goal"
    },
    "purpose": {
      "enum": [
        "screen",
        "characterize",
        "compare",
        "verify",
        "troubleshoot",
        "feed"
      ],
      "description": "Cannot be inferred; a missing purpose is a question for the human (4.5.1)"
    },
    "intent": {
      "enum": [
        "explore",
        "confirm"
      ],
      "description": "Defaults from purpose. Differing from the default requires intent_rationale (check 32)."
    },
    "intent_rationale": {
      "type": "string",
      "minLength": 10
    },
    "observable": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "definition": {
          "type": "string",
          "minLength": 20,
          "description": "Kept for now and no longer required: the definition lives in contracts/observables.json, and a card that restates it holds a second copy that drifts -- eight cards had already drifted from the vocabulary by 2026-09-18. Name the observable and the definition, the estimator and the window come with it by reference. This property is due to be removed once every card has dropped it; 5.4 still asks for the definition and that sentence is the architecture seat's to settle before then."
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "targets": {
      "type": "array",
      "description": "What accuracy is wanted. In explore mode, stated in decades.",
      "items": {
        "type": "object",
        "properties": {
          "metric": {
            "type": "string"
          },
          "number": {
            "type": "string"
          },
          "kind": {
            "enum": [
              "uncertainty",
              "decade_resolution",
              "detection"
            ]
          }
        },
        "required": [
          "metric",
          "number",
          "kind"
        ],
        "additionalProperties": false
      }
    },
    "priority": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Ordered. Safety is deliberately absent: it is a constraint, not a tradeable item (2.1)."
    },
    "compare_variable": {
      "type": "string",
      "description": "required when purpose is compare: the one parameter allowed to differ between arms (check 34)"
    },
    "requested_by": {
      "$ref": "common.schema.json#/$defs/agent"
    },
    "constraint_notes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Free-text limits from the human. Named apart from the interval lists elsewhere: same word, different kind of thing."
    },
    "configuration_preference": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_]*$"
      },
      "description": "An ordered tie-break from the person, read by S3.0 ONLY when the cap is unresolved (4.5.1 branch c). It settles which of several capable configurations to spend the fan-out on. It never overrides a capability refusal: a preference naming a configuration the screen rejected, or one absent from the table, is recorded and refused rather than honoured, and screening stops at that entry instead of sliding to the next -- a card asking for a configuration the instrument cannot use is a card to fix, and quietly granting its second choice hides that. It is not evidence: it raises no grade, supports no number, is not an assumption with a falsifier, and S4 may not cite it."
    },
    "sample_contrast_provenance": {
      "type": "object",
      "description": "Where sample_contrast came from. It is a claim about the world -- these beads carry a fluorophore -- so it is graded like any other claim (P2). An operator reading an instrument and an operator recalling a preparation are not the same source and do not get the same grade: a statement from memory is E5. Required in practice whenever sample_contrast is present; left optional for one step only, because making it required today would fail a goal card already committed in another seat's path. Tighten once that card carries it.",
      "properties": {
        "source": {
          "$ref": "common.schema.json#/$defs/source"
        },
        "grade": {
          "$ref": "common.schema.json#/$defs/grade"
        }
      },
      "required": [
        "source",
        "grade"
      ],
      "additionalProperties": false
    },
    "configuration_preference_by": {
      "type": "object",
      "description": "Who chose the tie-break, and when. Deliberately NOT a source and a grade: a preference is not a claim about the world, so there is nothing to grade and nothing to falsify (4.5.1 c). What it needs is attribution, so that a later reader can ask the person rather than mistake the choice for a finding. `reason` is optional and stays empty when none was given -- an invented reason would be worse than the silence.",
      "properties": {
        "who": {
          "type": "string",
          "minLength": 1
        },
        "at": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "who",
        "at"
      ],
      "additionalProperties": false
    },
    "sample_contrast": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "common.schema.json#/$defs/contrast"
      },
      "description": "What this sample offers, as a set. A configuration survives the contrast screen when its requires_contrast is one of these -- membership, not equality. A set because a fluorescent tracer still has refractive-index contrast and so provides both, which is why equality would drop brightfield from a fluorescent question. S3.0 can evaluate this before S3 runs, which none of the priority terms can (4.5.3)."
    },
    "from_round": {
      "type": "string",
      "pattern": "^thr-[a-z0-9-]+:r[1-9][0-9]*$",
      "description": "Present when this goal was written from a delivered round (5.1): the thread and round it came from. It is how the bridge learns the round was taken. The bridge already reads the executing agents' questions/ (6.2-3), so this closes the loop with no write across a boundary and no second copy of whose turn it is -- the thread ledger stays the only place that says that."
    }
  },
  "required": [
    "purpose",
    "intent",
    "observable",
    "requested_by"
  ],
  "unevaluatedProperties": false
}
