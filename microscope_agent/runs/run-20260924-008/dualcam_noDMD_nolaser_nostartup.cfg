# DERIVED for run-20260924-008 by src/session_036_readonly.py from
#   C:\agentic_microscope\config\micromanager\dualcam_noDMD.cfg (sha256 f8677764c83abeac4708869e0882308044b6cf52e55ae4c5c1d9c644392e3630)
# Removed: every line naming NIDAQHub or LUNF-Blanking, the LaserLine group,
# and the System/Startup preset. Nothing else changed.
# DERIVED FILE -- do not edit by hand.
# Regenerate:
#   sed '/^Device,MightexPolygon1000,/d' DMD_dualcam_LUNF.cfg > dualcam_noDMD.cfg
# Parent:  DMD_dualcam_LUNF.cfg
#
# Edits against the parent -- one removed line, plus the PixelSize block:
#
#   removed  Device,MightexPolygon1000,...
#     Same reason as single_cam_red_noDMD.cfg: the Mightex Polygon vendor
#     package is pinned to MM device interface v71 and lives only in the lab's
#     own C:\Program Files\Micro-Manager-2.0 install, so pymmcore-plus cannot
#     load it. It is not in the emission path and the dual-camera run does not
#     use it.
#
#   added    the PixelSize block, 2026-09-06, via
#            `python config/micromanager/set_pixel_size.py dualcam_noDMD.cfg
#             --objective <M> --write` for all six objectives.
#     The parent's block is present and EMPTY, so getPixelSizeUm() answered
#     0.0 on this configuration and anything asking the instrument for its
#     scale got nothing -- confirmed live 2026-09-06 with the 100x Oil in
#     place. single_cam_red_noDMD.cfg had the same gap closed on 2026-09-04;
#     this is the dual-camera equivalent, and it is why this file diverges
#     from its parent by more than the DMD line.
#     ⚠ These presets key on the Nosepiece ALONE and assume intermediate 1x,
#     because IntermediateMagnification has no Label lines to key on. At 1.5x
#     every value is high by exactly 1.5x. `IntermediateMagnification` is also
#     READ-ONLY over MM (one property, `Magnification`, no setter), so it is a
#     manual change at the stand. Close the gap with
#     `python -m calibration.cli intermediate-mag <this file>`.
#     Only the 20x row is measured (0.32373 vs a nominal 0.325, a real
#     20.078x); the other five are exactly 6.5/M. See data/pixel_size.yaml.
#
# WHY A DUAL-CAM noDMD VARIANT EXISTS (added 2026-09-06)
# -----------------------------------------------------
# This is the configuration for the two-colour simultaneous run: Kinetix_blue
# (Camera-1, reflect side of the splitter, Dragon Green) AND Kinetix_red
# (Camera-2, transmit side, the Abvigen red bead) loaded together. The three
# single-camera variants deliberately load one body each so the Tweez GUI can
# hold the other (SAFETY.md §4), which is the right choice for setup work and
# the wrong one here -- a two-colour measurement needs both bodies in ONE
# process, because PVCAM hands a Kinetix to one owner at a time.
#
# ⚠ CONSEQUENCE: WITH THIS CONFIG LOADED THE TWEEZ GUI CANNOT HAVE A CAMERA.
# Do the GUI-side work first -- trap placement, the px->um calibration that an
# objective change invalidates (SAFETY.md §2) -- then release, then load this.
#
# ⚠ THERE IS NO "Multi Camera" UTILITY DEVICE HERE. Core.Camera names one body
# (Kinetix_red below), so `snapImage` and `startSequenceAcquisition` address
# that one only. Driving both is a per-camera call on each label; simultaneity
# comes from the hardware trigger, not from MMCore doing it for you.
# Generated by Configurator on Fri Jul 03 15:32:35 PDT 2026

# Reset
Property,Core,Initialize,0

# Devices
Device,COM10,SerialManager,COM10
Device,Ti2-E__0,NikonTi2,Ti2-E__0
Device,ZDrive,NikonTi2,ZDrive
Device,XYStage,NikonTi2,XYStage
Device,Nosepiece,NikonTi2,Nosepiece
Device,CondenserTurret,NikonTi2,CondenserTurret
Device,FilterTurret1,NikonTi2,FilterTurret1
Device,Turret1Shutter,NikonTi2,Turret1Shutter
Device,FilterTurret2,NikonTi2,FilterTurret2
Device,Turret2Shutter,NikonTi2,Turret2Shutter
Device,LightPath,NikonTi2,LightPath
Device,PFS,NikonTi2,PFS
Device,PFSOffset,NikonTi2,PFSOffset
Device,IntermediateMagnification,NikonTi2,IntermediateMagnification
Device,LappMainBranch1,NikonTi2,LappMainBranch1
Device,DiaLamp,NikonTi2,DiaLamp
Device,Kinetix_blue,PVCAM,Camera-1
Device,LightEngine,Lumencor,LightEngine
Device,Kinetix_red,PVCAM,Camera-2
Device,CSUW1-Hub,CSUW1,CSUW1-Hub
Device,CSUW1-Filter_Red,CSUW1,CSUW1-Filter Wheel
Device,CSUW1-Filter_Blue,CSUW1,CSUW1-Filter Wheel
Device,CSUW1-Dichroic,CSUW1,CSUW1-Dichroic Mirror
Device,CSUW1-Shutter,CSUW1,CSUW1-Shutter
Device,CSUW1-Bright,CSUW1,CSUW1-Bright Field
Device,CSUW1-Port,CSUW1,CSUW1-Port
Device,Aura,Lumencor,LightEngine

# --- LUN-F XL fast blanking via NI PCIe-6323 (added 2026-08-19) ---

# Pre-init settings for devices
Property,XYStage,Invert-X,No
Property,XYStage,Invert-Y,No
Property,LightEngine,Connection,COM3
Property,LightEngine,Model,GEN3
Property,CSUW1-Hub,Port,COM10
Property,CSUW1-Filter_Red,WheelNumber,1
Property,CSUW1-Filter_Blue,WheelNumber,2
Property,Aura,Connection,COM7
Property,Aura,Model,GEN3

# --- LUN-F DAQ pre-init ---

# Pre-init settings for COM ports
Property,COM10,AnswerTimeout,8000
Property,COM10,BaudRate,115200
Property,COM10,DTR,Disable
Property,COM10,DataBits,8
Property,COM10,DelayBetweenCharsMs,0.0000
Property,COM10,Fast USB to Serial,Disable
Property,COM10,Handshaking,Off
Property,COM10,Parity,None
Property,COM10,StopBits,1
Property,COM10,Verbose,1

# Hub (parent) references
Parent,ZDrive,Ti2-E__0
Parent,XYStage,Ti2-E__0
Parent,Nosepiece,Ti2-E__0
Parent,CondenserTurret,Ti2-E__0
Parent,FilterTurret1,Ti2-E__0
Parent,Turret1Shutter,Ti2-E__0
Parent,FilterTurret2,Ti2-E__0
Parent,Turret2Shutter,Ti2-E__0
Parent,LightPath,Ti2-E__0
Parent,PFS,Ti2-E__0
Parent,PFSOffset,Ti2-E__0
Parent,IntermediateMagnification,Ti2-E__0
Parent,LappMainBranch1,Ti2-E__0
Parent,DiaLamp,Ti2-E__0

# Initialize
Property,Core,Initialize,1

# Delays

# Focus directions
# ======================================================================
# ZDrive: 1 = INCREASING Z MOVES TOWARD THE SAMPLE.
#
# Equivalently, smaller Z is the retracted direction -- measured by the
# operator 2026-09-05 and carried in code as
# `hardware.microscope.Z_RETRACT_DIRECTION = -1`. It stood here as 0
# ("unknown") until 2026-09-06 while the measurement sat in SAFETY.md
# prose and one Python constant, so MM itself could not answer which way
# retract was.
#
# ⚠ THIS IS A SAFETY-RELEVANT SIGN, AND IT REVERSED ONCE. Until
# 2026-09-05 this repository carried the opposite guess ("+Z is probably
# retracted"), inferred from a nosepiece rotation at ZDrive = 8288.740
# that broke nothing -- which under the measured convention had driven
# the incoming lens ~5.3 mm PAST the sample plane, so nothing broke
# because nothing was in the way. Anyone following the old guess would
# have driven the objective toward a coverslip 0.13 mm away believing
# they were retracting. SAFETY.md section 2 is the full account.
#
# WHY `1` AND NOT `-1` OR `2`: those are the two plausible encodings of
# this field and `1` means "toward sample" in BOTH of them -- as a sign
# (anything > 0 is toward the sample) and as the MM enum ordinal
# (0 Unknown, 1 TowardSample, 2 AwayFromSample). So this line does not
# rest on knowing which one MMCore parses. Do not "correct" it to 2 or
# -1 without first confirming the encoding against a live core; either
# would be a coin flip on a direction that swings glass at a coverslip.
#
# PFSOffset stays 0 = UNKNOWN, deliberately. Its sign convention has
# never been measured or stated -- nothing in this repository records
# which way its offset lens moves relative to the sample -- and it is in
# COLLISION_DEVICES alongside ZDrive and the Nosepiece. An unmeasured
# direction written here would read as verified.
# TODO(human): the PFSOffset sign, measured the way the ZDrive one was.
# ======================================================================
FocusDirection,ZDrive,1
FocusDirection,PFSOffset,0

# Roles
Property,Core,Camera,Kinetix_red
Property,Core,Shutter,LightEngine
Property,Core,AutoShutter,1

# Camera-synchronized devices

# Labels
# Nosepiece
Label,Nosepiece,5,6-Plan Apo LmbdD0.13 100x Oil
Label,Nosepiece,4,5-Plan Apo LmbdD0.15 60x Oil
Label,Nosepiece,3,4-Apo LmbdS 40x WI
Label,Nosepiece,2,3-Plan Apo LmbdD0.8 20x
Label,Nosepiece,1,2-Plan Apo LmbdD4 10x
Label,Nosepiece,0,1-Plan Apo LmbdD20 4x
# CondenserTurret
Label,CondenserTurret,6,7-
Label,CondenserTurret,5,6-
Label,CondenserTurret,4,5-
Label,CondenserTurret,3,4-
Label,CondenserTurret,2,3-
Label,CondenserTurret,1,2-Shutter
Label,CondenserTurret,0,1-ND
# FilterTurret1
Label,FilterTurret1,5,6-Empty
Label,FilterTurret1,4,5-Empty
Label,FilterTurret1,3,4-Empty
Label,FilterTurret1,2,3-Empty
Label,FilterTurret1,1,2-Empty
Label,FilterTurret1,0,1-MXR00724 -Empty
# FilterTurret2
Label,FilterTurret2,5,6-Empty
Label,FilterTurret2,4,5-Empty
Label,FilterTurret2,3,4-Empty
Label,FilterTurret2,2,3-Empty
Label,FilterTurret2,1,2-Empty
Label,FilterTurret2,0,1-Empty
# LightPath
Label,LightPath,3,4-L100
Label,LightPath,2,3-AUX
Label,LightPath,1,2-R100
Label,LightPath,0,1-EYE
# LappMainBranch1
Label,LappMainBranch1,1,mirror_in
Label,LappMainBranch1,0,mirror_out
# CSUW1-Filter_Red
Label,CSUW1-Filter_Red,9,open
Label,CSUW1-Filter_Red,8,b4
Label,CSUW1-Filter_Red,7,b3
Label,CSUW1-Filter_Red,6,b2
Label,CSUW1-Filter_Red,5,b1
Label,CSUW1-Filter_Red,4,647
Label,CSUW1-Filter_Red,3,555
Label,CSUW1-Filter_Red,2,488
Label,CSUW1-Filter_Red,1,405
Label,CSUW1-Filter_Red,0,multi
# CSUW1-Filter_Blue
Label,CSUW1-Filter_Blue,9,open
Label,CSUW1-Filter_Blue,8,b4
Label,CSUW1-Filter_Blue,7,b3
Label,CSUW1-Filter_Blue,6,b2
Label,CSUW1-Filter_Blue,5,b1
Label,CSUW1-Filter_Blue,4,647
Label,CSUW1-Filter_Blue,3,555
Label,CSUW1-Filter_Blue,2,488
Label,CSUW1-Filter_Blue,1,405
Label,CSUW1-Filter_Blue,0,multi
# CSUW1-Dichroic
Label,CSUW1-Dichroic,2,b2
Label,CSUW1-Dichroic,1,b1
Label,CSUW1-Dichroic,0,on
# CSUW1-Bright
Label,CSUW1-Bright,1,Bright Field
Label,CSUW1-Bright,0,Confocal
# CSUW1-Port
Label,CSUW1-Port,2,red_only
Label,CSUW1-Port,1,blue_red
Label,CSUW1-Port,0,blue_only

# Configuration presets
# Group: Channel

# Group: LaserLine  (405/488/561/640 blanking; power stays on the LUN-F FT4222 DAC)

# Group: System
# Preset: Startup
# ======================================================================
# WHY THESE ARE PINNED BY `State` AND NOT BY `Label`.
#
# The photometry is settled and is independent of every name below:
# LappMainBranch1 State 1 passes the Lapp-branch light to the sample
# (p99.9 21083 ADU), State 0 blocks it completely (139, identical to a
# dark frame). Measured 2026-09-04, re-confirmed independently
# 2026-09-06 through a different filter at a different exposure.
#
# The NAMES for those two states were swapped in this file until
# 2026-09-06 and are now `1 = mirror_in`, `0 = mirror_out` per the
# operator (2026-09-05) -- a mirror *inserted* is what couples the side
# branch in, which is also the physically sensible reading. Because that
# attribution moved once, a preset written as `...,Label,mirror_out`
# would have silently selected the BLOCKING state the day it moved.
# Pinning the integer makes this line survive any further renaming.
#   kb/systems/dualcam-config-corrections-pending.md item 1
#   kb/systems/current.md > lapp_branch
# ======================================================================
#
# CSUW1-Port State 1 = `blue_red` -- dual-camera, operator-stated 2026-09-05.
# Until now no Property or ConfigGroup line set this at load, so the
# port stayed wherever it physically was; on 2026-09-05 that was
# red_only, i.e. the file named "dualcam" did not put the instrument
# in dual-camera mode and Kinetix_blue simply received no light, with
# nothing erroring.  (pending-corrections item 2)



# PixelSize settings


ConfigPixelSize,4x-1x,Nosepiece,Label,1-Plan Apo LmbdD20 4x
PixelSize_um,4x-1x,1.625
ConfigPixelSize,10x-1x,Nosepiece,Label,2-Plan Apo LmbdD4 10x
PixelSize_um,10x-1x,0.65
ConfigPixelSize,20x-1x,Nosepiece,Label,3-Plan Apo LmbdD0.8 20x
PixelSize_um,20x-1x,0.32373
ConfigPixelSize,40x-1x,Nosepiece,Label,4-Apo LmbdS 40x WI
PixelSize_um,40x-1x,0.1625
ConfigPixelSize,60x-1x,Nosepiece,Label,5-Plan Apo LmbdD0.15 60x Oil
PixelSize_um,60x-1x,0.10833
ConfigPixelSize,100x-1x,Nosepiece,Label,6-Plan Apo LmbdD0.13 100x Oil
PixelSize_um,100x-1x,0.065
